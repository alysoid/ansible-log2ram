---
- name: Clean systemd journal
  command: journalctl --vacuum-size={{ ((log2ram_size[:-1] | float) * 0.95) | int }}{{ log2ram_size_suffix }}

- name: Restart log2ram
  service:
    name: log2ram
    state: restarted
